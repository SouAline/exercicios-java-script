let firstNumber = window.prompt("Enter a number:")
let secondNumber = window.prompt("Enter other number:")
if (firstNumber > secondNumber) {
    window.alert(`The ${firstNumber} it's bigger`)
} else {
    window.alert(`The ${secondNumber} 
    it's smaller`)
}